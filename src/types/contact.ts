export interface Contact {
    id: string,
    name: string,
    relationship: string,
    location: string,
    category: string,
    priority: string,
    lastEntry: string,
}

export interface Category {
    id: string,
    name: string,
}
